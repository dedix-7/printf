#include <stdio.h>
#include "main.h"
/**
 * main -test code
 * Return: 0 on success
 */
int main(void)
{
	int len, len2;
	char *c = "Let's check with a simple string";

	len =  _printf("your printf % prints %s character\n", c);
	len2 = printf("orig printf % prints %s character\n", c);

	printf("Your printf returns %d\n thee originala printf return %d\n", len, len2);
	return(0);
}
